# Match the repo-wide convention: dependency peer ranges are resolved with
# --legacy-peer-deps (see root install / docs-site). Required so `npm ci` in CI
# reproduces the committed lockfile without peer-conflict failures.
legacy-peer-deps=true
registry=https://registry.npmjs.org/
